/**
 * Web Routes (like Laravel routes/web.php)
 * Non-API, server-rendered or web-only routes. API lives in routes/api.js.
 */

import { Router } from 'express';

const router = Router();

// Example: router.get('/health-page', (req, res) => res.send('OK'));

export default router;
